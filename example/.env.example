# AWS Profile (optional, if not using default profile)
# AWS_PROFILE=your-aws-profile

# AWS Region (optional, defaults to ap-northeast-1)
# AWS_REGION=ap-northeast-1

# KMS Key ID (optional, automatically read from amplify_outputs.json)
# Only set this if you're not using Amplify Gen2
# KMS_KEY_ID=your-kms-key-id-here

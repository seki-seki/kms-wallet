{
  "name": "kms-wallet",
  "version": "1.0.0",
  "description": "Ethereum wallet library using AWS KMS for signing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepare": "npm run build"
  },
  "keywords": [
    "ethereum",
    "kms",
    "aws",
    "wallet",
    "signing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-kms": "^3.0.0",
    "ethers": "^6.0.0"
  }
}
